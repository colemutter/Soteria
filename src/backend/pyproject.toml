[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apache-airflow>=3.2.2",
    "pendulum>=3.2.0",
    "psycopg[binary]>=3.2",
]
